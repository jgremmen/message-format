# messageFormatPack Task

TODO: Document the `MessageFormatPackTask` Gradle task. Cover class file scanning, include/exclude
regex filters, importing existing `.mfp` files, duplicate message strategies (`FAIL`,
`IGNORE_AND_WARN`, `REPLACE`), template validation, custom `MessageAccessor` actions, and
using the generated `.mfp` file at runtime via `importMessages`.
