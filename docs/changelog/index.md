---
icon: material/update
---

# Upgrade

Each version has its own changelog page that describes additions, removals, bug fixes and
breaking changes in detail. Where behavior or API has changed, the changelog provides before
and after examples so you can see exactly what needs to be adjusted when migrating to a newer
version.
