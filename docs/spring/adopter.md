# SpringAsmAnnotationAdopter

TODO: Document the Spring ASM-based annotation adopter. Covers scanning via Spring's bundled
ASM library, `ResourceLoader` integration, classpath pattern support, and the same annotation
detection as `message-format-asm` without requiring an external ASM dependency.
