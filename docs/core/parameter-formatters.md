# Parameter Formatters

TODO: Document type-based resolution (`FormattableType`, priority/order), named formatters
(`NamedParameterFormatter`), and implementing custom formatters. Cover `MapKeyComparator`,
`SizeQueryable`, and classifier support.

For detailed reference of each built-in formatter, see the
[Formatter Reference](../formatter/typed.md).
