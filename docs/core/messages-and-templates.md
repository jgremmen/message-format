# Messages and Templates

TODO: Document the `Message` interface hierarchy (`WithSpaces`, `WithCode`, `LocaleAware`),
adding messages and templates to `ConfigurableMessageSupport`, `MessageFactory` parsing methods,
`MessageBuilder` for programmatic construction, message/template filters, and
`findMissingTemplates`.
