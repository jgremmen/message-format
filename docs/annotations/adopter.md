# Annotation Adopter Base

TODO: Document `AbstractAnnotationAdopter` -- the base class shared by the ASM and Spring
adopters. Covers the `adopt(MessageDef)` and `adopt(TemplateDef)` methods for processing
synthesized or mocked annotations directly.
