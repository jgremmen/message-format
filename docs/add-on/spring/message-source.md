# MessageSupportMessageSource

TODO: Document the Spring `HierarchicalMessageSource` implementation that delegates to
`MessageSupport`. Cover positional argument mapping to named parameters (`p1`, `p2`, ...),
configurable parameter prefix, and parent message source fallback.
